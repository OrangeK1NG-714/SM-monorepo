# 师生双选系统的设计与实现

## 目录

- 研究背景与意义
- 需求分析
- 系统架构设计
- 数据库设计
- 核心功能展示
- 关键技术实现
- 系统测试
- 总结与展望

---

## 研究背景与意义

### 现状与痛点

- 传统师生双选依赖线下沟通、纸质表格或 Excel 手工统计，效率低、易出错
- 信息不对称：学生难以全面了解导师方向与名额，导师难以横向对比学生意愿
- 多轮选择流程复杂，时间节点管理困难，缺乏自动化通知机制

### 研究目标

- 设计并实现一套 **线上师生双选系统**，覆盖学生选导师、导师选学生的完整双向匹配流程
- 支持多轮分时选择、自动微信消息通知、管理后台统一调度
- 采用移动端优先（微信小程序）+ Web 管理后台的 B/S 架构

---

## 需求分析

### 用户角色

| 角色 | 核心需求 |
|------|---------|
| **学生** | 填写个人信息、浏览导师列表、按优先级选择 3 位导师、查看匹配结果 |
| **导师** | 查看选择自己的学生、分轮次择优录取、查看已录取学生 |
| **管理员** | 管理用户账号、创建/配置选择活动、设置时间窗口、查看与导出数据 |

### 功能需求

- 微信小程序端：登录、个人信息管理、导师浏览与筛选、志愿提交、结果查询
- 管理后台：用户 CRUD、活动生命周期管理、志愿数据查看与导出、导师名额配置
- 后端服务：RESTful API、JWT 认证鉴权、定时任务、微信消息推送

### 非功能需求

- 移动端适配（微信小程序 + H5）
- 角色权限隔离
- 选择时间窗口的服务端校验，防止前端绕过

---

## 系统架构设计

### 整体架构

```
┌─────────────────────────────────────────────────┐
│                   用户层                         │
│  ┌──────────────────┐  ┌──────────────────────┐ │
│  │  微信小程序 / H5  │  │   管理后台（Web）     │ │
│  │  UniApp + Vue 3   │  │  Vue 3 + Element Plus │ │
│  └────────┬─────────┘  └──────────┬───────────┘ │
├───────────┼────────────────────────┼─────────────┤
│           │        网关层          │             │
│           │   Nginx 反向代理       │             │
├───────────┼────────────────────────┼─────────────┤
│           ▼        服务层          ▼             │
│  ┌──────────────────────────────────────────┐   │
│  │         Egg.js 后端服务                   │   │
│  │  · RESTful API（约 40 个接口）            │   │
│  │  · JWT 双 Token 认证                     │   │
│  │  · 定时任务调度                           │   │
│  │  · 微信服务集成                           │   │
│  └────────────────────┬─────────────────────┘   │
├───────────────────────┼─────────────────────────┤
│                       ▼      数据层              │
│  ┌──────────────┐  ┌────────────────────────┐   │
│  │   MongoDB     │  │   微信开放平台 API      │   │
│  │  （7 个集合）  │  │  （openid / 订阅消息）  │   │
│  └──────────────┘  └────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

### 技术选型

| 层级 | 技术栈 |
|------|--------|
| 小程序端 | UniApp + Vue 3 + TypeScript + Vite + Pinia + UnoCSS |
| 管理后台 | Vue 3 + Vite + Element Plus + Vuex + Axios |
| 后端服务 | Egg.js (Node.js) + Mongoose + JWT + egg-validate |
| 数据库 | MongoDB |
| 工程化 | pnpm Monorepo + ESLint + Husky + Commitlint |
| 部署 | Nginx + PM2 + 宝塔面板 |

### 选型理由

- **UniApp**：一套代码同时支持微信小程序和 H5，降低开发与维护成本
- **Egg.js**：企业级 Node.js 框架，内置定时任务、插件机制，适合中型项目
- **MongoDB**：文档型数据库，Schema 灵活，适合存储结构多变的学生/导师信息
- **pnpm Monorepo**：前后端统一管理，共享配置，一键启动开发环境

---

## 数据库设计

### 核心数据模型

| 集合名 | 用途 | 关键字段 |
|--------|------|---------|
| **UserInfo** | 用户账号 | username, password, role(admin/student/teacher) |
| **Student** | 学生档案 | studentId, data(姓名/绩点/方向等), mentor, openid |
| **Teacher** | 导师信息 | teacherId, name, teacherType(专业/公共/校友), allowedMajors |
| **Activity** | 选择活动 | 起止时间, 学生选择窗口, 3 轮导师选择窗口 |
| **Choose** | 志愿记录 | studentId, teacherId, order(1-3), isChose, activityId |
| **Final** | 最终匹配 | activityId, studentId, teacherId, order |
| **UserInActivity** | 活动参与者 | userId, activityId, maxSelectNum |

### 数据关系图

```
UserInfo ──1:1──▶ Student / Teacher
    │
    └──1:N──▶ UserInActivity ──N:1──▶ Activity
                                         │
Student ──1:N──▶ Choose ──N:1──▶ Teacher  │
                   │                      │
                   └──────N:1─────▶ Activity
                   
Activity ──1:N──▶ Final ◀──N:1── Student
                         ◀──N:1── Teacher
```

---

## 核心功能展示

### 学生端 — 导师选择

- 导师按类型分类浏览（专业导师 / 公共导师 / 校友导师）
- 根据学生专业类型（普通/中本）自动过滤可选导师
- 选择 3 位导师并设置志愿优先级（第一 / 第二 / 第三志愿）
- 实时显示导师剩余名额

> 📱 演示截图：导师列表页 → 志愿排序页 → 提交成功页

### 导师端 — 学生选择

- 按志愿优先级分组查看选择自己的学生
- 三轮分时选择机制，每轮有独立的时间窗口
- 名额上限控制，已满则无法继续选择
- 可查看学生是否已被其他导师录取

> 📱 演示截图：学生列表页 → 选择操作 → 已选学生页

### 管理后台

- 活动创建：配置 8 个时间节点（总起止 + 学生选择窗口 + 3 轮导师选择窗口）
- 用户管理：批量导入、角色分配、密码重置
- 数据查看：志愿列表、最终匹配结果、Excel 导出

> 🖥️ 演示截图：活动管理页 → 用户列表页 → 数据导出页

### 微信消息通知

- 学生在小程序端授权订阅消息
- 第三轮选择结束后，定时任务自动触发
- 被录取学生收到"恭喜被 XX 老师选中"通知
- 未被录取学生收到对应提醒

---

## 关键技术实现

### 1. JWT 双 Token 认证机制

```
客户端                        服务端
  │── 登录请求 ──────────────▶│
  │◀── accessToken(短期)  ───│
  │◀── refreshToken(长期) ───│
  │                           │
  │── 携带 accessToken 请求 ─▶│
  │◀── 正常响应 ─────────────│
  │                           │
  │── accessToken 过期 ──────▶│── 返回 401
  │── 携带 refreshToken ─────▶│── 签发新 accessToken
  │◀── 新 accessToken ───────│
```

- 防止 401 状态下重复跳转登录页的竞态处理
- 角色鉴权中间件（admin / student / teacher 三级权限）

### 2. 多轮分时选择机制

```
时间轴 ──────────────────────────────────────────▶

 [学生选择窗口]
  ├── 学生提交 3 个志愿 ──┤

              [第一轮导师选择]  [第二轮]  [第三轮]
               ├─ 导师选人 ─┤  ├──────┤  ├──────┤
```

- 服务端严格校验当前时间是否在对应窗口内，防止前端篡改
- 每轮选择独立计数，互不影响
- 第三轮结束自动触发微信通知

### 3. 微信订阅消息推送

- 定时任务每分钟检测第三轮是否结束
- 通过 `wx.login()` 获取的 openid 定向推送
- access_token 缓存机制，避免频繁请求微信 API
- `subscribeSent` 标记防止重复发送

### 4. 专业类型匹配过滤

- 学生分为"普通"和"中本"两种专业类型
- 导师设置 `allowedMajors` 字段声明可带专业类型
- 前后端双重过滤，确保学生只能选择对应类型的导师

---

## 系统测试

### 测试环境

- 操作系统：Windows 11 / CentOS（服务端）
- 浏览器：Chrome、微信开发者工具
- 数据库：MongoDB 本地 / 云服务器

### 功能测试

| 测试模块 | 测试内容 | 结果 |
|----------|---------|------|
| 用户登录 | 学号/工号登录、错误密码提示、Token 刷新 | ✅ 通过 |
| 学生选导师 | 志愿提交、优先级排序、时间窗口限制 | ✅ 通过 |
| 导师选学生 | 分轮选择、名额控制、已录取状态显示 | ✅ 通过 |
| 管理后台 | 活动 CRUD、用户管理、数据导出 | ✅ 通过 |
| 消息推送 | 订阅授权、定时触发、消息送达 | ✅ 通过 |

### 兼容性测试

- 微信小程序（iOS / Android）
- H5（Chrome / Safari）
- 管理后台（Chrome / Edge / Firefox）

---

## 总结与展望

### 项目总结

- 实现了覆盖 **学生、导师、管理员** 三个角色的完整师生双选流程
- 采用 **Monorepo** 架构统一管理前后端 3 个应用
- 核心亮点：多轮分时选择机制、JWT 双 Token 认证、微信订阅消息自动推送
- 系统已部署上线并完成功能测试

### 不足与展望

| 方向 | 改进内容 |
|------|---------|
| 实时性 | 完善 SSE/WebSocket 推送，选择状态实时更新 |
| 数据分析 | 增加可视化数据看板（选择热度、匹配率等） |
| 智能匹配 | 引入推荐算法，辅助学生选择合适导师 |
| 移动端 | 导师端也适配小程序，减少对 Web 的依赖 |
| 安全性 | 增加接口限流、操作日志审计 |

---

## 致谢

感谢指导老师的悉心指导，感谢同学们在测试阶段提供的宝贵反馈。

---

> 答辩人：XXX ｜ 指导老师：XXX ｜ 浙江科技大学 · 数字媒体技术
